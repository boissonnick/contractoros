/**
 * Messaging Components
 *
 * Shared components for message threads across the application.
 * Used in client portal, project messages, and other messaging contexts.
 */

export { MessageThread, default } from './MessageThread';
export type {
  Message,
  MessageAttachment,
  MessageThreadProps,
  UploadProgressInfo,
} from './MessageThread';
