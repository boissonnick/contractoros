import { ComingSoon } from '@/components/ui';

export default function ProjectsInboxPage() {
  return <ComingSoon title="Inbox" description="View messages, notifications, and updates across all projects." />;
}
